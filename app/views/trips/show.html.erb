<p>
	<strong>Title:</strong>
	<%= @trip.title %>
</p>
<p>
	<strong>Address:</strong>
	<%= @trip.address %>
</p>

<h2>Courses</h2>
<%= render @trip.courses %>

<h2>Add a course:</h2>
<%= render 'courses/form' %>
 
<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>